# macOS Finder metadata — never commit these
.DS_Store
**/.DS_Store

# Node / build artifacts (in case any tooling runs here)
node_modules/

# Editor cruft
*.swp
Thumbs.db
Claude outputs/

# PowerPoint sources — ~90-100 MB each, over GitHub's file limit. Kept locally.
*.pptx
# Office lock files
~$*
